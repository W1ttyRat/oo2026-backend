# oo2026-backend

Docker configuration for the oo2026 backend application.

## Docker Build

Build the Docker image:

```bash
docker build -t oo2026-backend .
```

Run the container:

```bash
docker run -p 8080:8080 oo2026-backend
```
